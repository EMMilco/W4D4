<h2><%= @album.title %></h2>
<h3><%= @album.band.name %></h3>

<h3>Track Listing:</h3>

<ul>
<% @album.tracks.each do |track| %>
    <li><a href="<%= track_url(track) %>"><%= track.title %></a></li>
<% end %>
</ul>

<a href="<%= new_album_track_url(@album) %>">Add a Track</a>
