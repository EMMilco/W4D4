<h1><%= @band.name %></h1>

<p><a href="<%= new_band_album_url(@band.id)%>">Add An Album</a></p>

<ul>
  <% @band.albums.each do |album| %>
      <li><a href="<%= album_url(album) %>"><%= album.title %></a></li>
  <% end %>
</ul>
